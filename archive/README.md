# Legacy artifacts

`legacy_flask_templates/` contains the pre-migration Flask/Jinja pages for historical reference only. No runtime code loads these files, and they are not part of the active chatbot journey.

The active frontend is `frontend/index.html`, served by FastAPI at `/` with its CSS and JavaScript under `/static`.
